# Tracer API — environment variable reference.
# Copy to .env and fill in real values for production.

NODE_ENV=development
PORT=3000

DATABASE_URL=postgres://tracer:tracer@localhost:5433/tracer
DB_LOGGING=false
DATABASE_SSL=false

# Required — minimum 16 characters. Change before deploying to production.
JWT_SECRET=change-me-32+chars-min-16-required
JWT_EXPIRES_IN=7d

CORS_ORIGINS=http://localhost:4200

LOG_LEVEL=info

# URL_PREVIEW=stub|real (default: real; auto-stub when NODE_ENV=test)
URL_PREVIEW=real
